HOST=127.0.0.1
PORT=4173

# Foreign servers and any server that passes direct connectivity tests should
# keep UPSTREAM_PROXY unset. Set it only when target sites are unreachable.
# The proxy must be reachable from this Linux server and use HTTP/HTTPS.
# UPSTREAM_PROXY=http://127.0.0.1:7897
